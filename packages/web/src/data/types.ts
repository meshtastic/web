/**
 * Shared database types
 */

/**
 * Type of conversation/message target
 * - "direct": Direct message to a specific node
 * - "channel": Broadcast message to a channel
 */
export type ConversationType = "direct" | "channel";
